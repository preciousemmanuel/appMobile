import React, { Component } from "react";
import { Image, Dimensions } from "react-native";

const LocaleImage = ({ source, originalHeight, originalWidth }) => {
  //const windowWidth;
  return <Image />;
};

export default LocaleImage;
